| Story ID                                          | E2E test(s) that cover it                                                                                            | Demo-mode manual verification steps (/?agentDemo=1)                                                                                                  | Notes / risks                                                                                     |
| ------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| **H1 – Home Snapshot**                            | `tests/e2e/regression.spec.js` (R.1), `tests/e2e/expenses-flow.spec.js` (B.1), `tests/e2e/stability.spec.js`         | Open app → `nav-home` → confirm “My Balance/Starting Balance” shows a currency amount. Add an account in Settings → return Home → balance increases. | Balance depends on summed account fields (`currentBalance*` vs `balance*`) and hydration timing.  |
| **H2 – Bills Due Indicator**                      | **Not covered in E2E** (unit only: `tests/unit/homeBillsDueAmount.test.mjs`)                                         | Seed bills + paid flags (needs store injection) → Home → “My Bills Due” reflects only unpaid share.                                                  | Known gap; requires deterministic store seeding via `window.__cashflowStore.setFullPlanData`.     |
| **P1 – Planner Landing**                          | `tests/e2e/planner-flow.spec.js` (D.1)                                                                               | `nav-planner` → confirm “Financial Analysis” heading and a planned chart/section is visible.                                                         | Planner can be sensitive to empty data; ensure hydration complete.                                |
| **P2 – Planned vs Actual Toggle**                 | `tests/e2e/planner-flow.spec.js` (D.2)                                                                               | `nav-planner` → toggle Planned/Actual → both remain visible and switch without errors.                                                               | UI state/selected styling can vary; validate functional change (numbers/labels).                  |
| **P3 – Actual Overlays Expenses**                 | `tests/e2e/regression.spec.js` (R.3, R.6)                                                                            | Record Planned/Actual End Balances → add expense via `nav-add` → return Planner → Planned ~unchanged; Actual decreased.                              | Uses tolerance; can drift if baseline data changes.                                               |
| **P4 – Week Rows Stay Consistent**                | `tests/e2e/regression.spec.js` (R.6)                                                                                 | `nav-planner` → scroll to Week 1 row → verify non-zero values and start+net≈end (if shown).                                                          | Depends on table rendering and formatted currency parsing.                                        |
| **B1 – Add Bill**                                 | `tests/e2e/regression.spec.js` (R.1), `tests/e2e/stability.spec.js`                                                  | `nav-bills` → Add Bill → fill Name/Amount/(Day) → Save → bill appears.                                                                               | Add flow differs based on empty state vs header; test handles both.                               |
| **B2 – Mark Bill Paid/Unpaid**                    | `tests/e2e/bills-flow.spec.js` (C.1)                                                                                 | `nav-bills` → toggle unpaid icon → verify paid indicator appears; toggle back if supported.                                                          | Selectors may be icon-class based; can be brittle if SVG classes change.                          |
| **X1 – Add Transaction from FAB**                 | `tests/e2e/expenses-flow.spec.js` (B.1), `tests/e2e/regression.spec.js` (R.3)                                        | `nav-add` → enter amount + description → Save → modal closes.                                                                                        | Modal input types vary; tests use fallback locators.                                              |
| **X2 – View Transactions List**                   | `tests/e2e/expenses-flow.spec.js` (B.1), `tests/e2e/persistence.spec.js` (F.2)                                       | Add a transaction → `nav-expenses` → verify it appears grouped by date with amount.                                                                  | Rendering can lag behind hydration; ensure store ready.                                           |
| **X3 – Edit or Delete Transaction**               | `tests/e2e/stability.spec.js`                                                                                        | `nav-expenses` → open a transaction → edit amount → save → verify update; delete → row removed.                                                      | Depends on edit sheet UI controls and confirm dialogs remaining stable.                           |
| **A1 – Add Account in Settings**                  | `tests/e2e/regression.spec.js` (R.1, R.6), `tests/e2e/stability.spec.js`                                             | `nav-settings` → Accounts & Residual → Add Account → Save → Home/Accounts show updated balance.                                                      | Multiple balance fields exist; normalization matters.                                             |
| **A2 – Accounts Listing**                         | `tests/e2e/regression.spec.js` (R.5)                                                                                 | Add an account → `nav-accounts` → confirm account card shows name + formatted currency.                                                              | Currency formatting differences (commas/decimals) can cause brittle assertions if locale changes. |
| **BU1 – Create Budget Category**                  | `tests/e2e/regression.spec.js` (R.3), `tests/e2e/expenses-flow.spec.js` (B.1), `tests/e2e/persistence.spec.js` (F.2) | `nav-settings` → Budgets → Add Category → Save → reload/navigate back → persists.                                                                    | Category selection in Add Transaction may vary depending on UI.                                   |
| **G1 – Create Goal**                              | `tests/e2e/regression.spec.js` (R.2), `tests/e2e/stability.spec.js`                                                  | `nav-settings` → Goals → Add Goal → Save → appears in list.                                                                                          | Inputs must remain visible/ordered; test uses `.last()`.                                          |
| **G2 – Goal Persistence on Reload**               | `tests/e2e/regression.spec.js` (R.2)                                                                                 | Create goal → reload → go back to Goals → values still present.                                                                                      | Persist/hydration timing is the typical failure mode.                                             |
| **AR1 – Save Allocation Rule**                    | `tests/e2e/regression.spec.js` (R.4)                                                                                 | `nav-settings` → Allocation Rules → Add Rule → set label/value → Save → reload → persists.                                                           | Rule schema changes could break locators (e.g., if label default changes).                        |
| **S1 – Update Starting Balance**                  | `tests/e2e/stability.spec.js`                                                                                        | `nav-settings` → Accounts & Residual → set Starting Balance → Save → Home/Planner reflect baseline when no accounts.                                 | Interacts with account balances; ensure test isolates state.                                      |
| **S2 – Update Income & Pay Schedule**             | `tests/e2e/stability.spec.js`                                                                                        | `nav-settings` → Income & Pay Schedule → enter incomes + pay days → Save → reload → persisted.                                                       | Pay schedule fields may be conditional on schedule type.                                          |
| **PH1 – Agent Demo Hydration**                    | `tests/e2e/expenses-flow.spec.js` (B.1), `tests/e2e/bills-flow.spec.js` (C.1), `tests/e2e/regression.spec.js`        | Load `/?agentDemo=1` → nav visible (home/add/etc) → app usable without auth.                                                                         | Hydration is historically fragile; recent fixes improved determinism.                             |
| **PH2 – Persisted Plan Reload**                   | `tests/e2e/regression.spec.js` (R.7)                                                                                 | Ensure seeded accounts/tx exist → reload → they still appear in Accounts/Expenses → hydration complete.                                              | The most timing-sensitive path; depends on IndexedDB persist + demo bootstrap.                    |
| **AI1 – Auto-post Paychecks on Payday**           | `tests/e2e/regression.spec.js` (R.7) + unit `tests/unit/autoPostPaychecks.test.mjs`                                  | Set mock date to payday → reload → salary tx appears in Expenses and balance increases.                                                              | In live demo manual mode, requires setting `localStorage.e2e-mock-today` then reloading.          |
| **AI2 – Auto-post Updates Balances Once Per Day** | `tests/e2e/regression.spec.js` (R.7) + unit `tests/unit/autoPostPaychecks.test.mjs`                                  | Trigger auto-post once → reload same day → no duplicate salary rows; balance unchanged on re-run.                                                    | Dedup keyed by `lastAutoPostRunISO` / `sourceKey`; timezone affects “today”.                      |
